//
// VersionIssuesStatus.swift
//
// Generated by swagger-codegen
// https://github.com/swagger-api/swagger-codegen
//

import Foundation


/** If the expand option &#x60;issuesstatus&#x60; is used, returns the count of issues in this version for each of the status categories *to do*, *in progress*, *done*, and *unmapped*. The *unmapped* property contains a count of issues with a status other than *to do*, *in progress*, and *done*. */

public struct VersionIssuesStatus {

    /** Count of issues with status *done*. */
    public let done: Int64?
    /** Count of issues with status *in progress*. */
    public let inProgress: Int64?
    /** Count of issues with status *to do*. */
    public let toDo: Int64?
    /** Count of issues with a status other than *to do*, *in progress*, and *done*. */
    public let unmapped: Int64?

    public init(done: Int64? = nil, inProgress: Int64? = nil, toDo: Int64? = nil, unmapped: Int64? = nil) {
        self.done = done
        self.inProgress = inProgress
        self.toDo = toDo
        self.unmapped = unmapped
    }

    internal init(client: Components.Schemas.VersionIssuesStatus?) {
        self.done = client?.done
        self.inProgress = client?.inProgress
        self.toDo = client?.toDo
        self.unmapped = client?.unmapped
    }
}
